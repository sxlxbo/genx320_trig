前置检查：

信号线：树莓派 GPIO 17 (物理11) -> 相机 J3-1

共地线：树莓派 GND -> 相机 J3-4

新增： 软屏 DIN -> 树莓派 GPIO 10 (物理19)

新增： 软屏独立供电，且其 GND 也连接到树莓派 GND 上。

## 📺 终端 1（接收端）：
### 启动监听：

cd ~/genx320_test/
python3 receive_record.py

此时屏幕显示：已开启数据预录制，等待第一个 LED 同步脉冲...

## 📺 终端 2（控制端）：开启一个新的终端，启动光源和触发：
cd ~/genx320_test/
python3 sync_led_trig.py

此时，软屏开始以 10Hz 的频率闪烁切换颜色。终端 1 会立刻响应并打印微秒级时间戳，开始倒计时录制。

## 💻 电脑端（提取数据）：
实验结束后，在 Windows 本地的 CMD 窗口（不要在 SSH 里）执行以下命令拉取文件：

拉取单个文件：
scp pi@raspberrypi.local:/home/pi/genx320_test/trigger_records/你的文件名.raw %USERPROFILE%\Desktop\
拉取整个文件夹：
scp -r pi@raspberrypi.local:/home/pi/genx320_test/trigger_records %USERPROFILE%\Desktop\

## 要增加树莓派发送 Trig 和切换 LED 灯的持续时间（即增加总的事件数量），你需要分别修改两个脚本中的总时长参数。
1. 发送端脚本 (sync_led_trig.py)
在这个脚本的开头部分，找到 # ================= 配置参数 ================= 区域。你需要修改的参数是 TOTAL_DURATION。

# ================= 配置参数 =================
TRIGGER_OUT_PIN = 17    # 触发输出引脚
LED_COUNT = 256         # 16x16 软屏
TOTAL_DURATION = 60.0   # 🎯 修改这里：你想运行的总时长（单位：秒）。比如改成 60.0 秒
INTERVAL = 0.1          # 切换间隔 (10Hz)。如果你想让它闪得更快或更慢，可以改这个。
# ============================================
提示：如果你把时长改为 60.0，且间隔保持 0.1，那么总共会产生 60 / 0.1 = 600 个 Trig 脉冲和颜色切换事件。

2，接收监视脚本 (receive_record.py)
在这个脚本中，向下滚动找到 trigger_recording(device) 这个函数。你需要修改其中的 record_duration 变量。

def trigger_recording(device):
    # 🎯 修改这里：将其设置为与 TOTAL_DURATION 相等，或者大 1~2 秒
    record_duration = 62  # 比如发送端设了 60 秒，这里可以设为 61 或 62 秒作为安全缓冲
    
    save_dir = "./trigger_records"
    os.makedirs(save_dir, exist_ok=True)
    # ... 下面的代码保持不变 ...


